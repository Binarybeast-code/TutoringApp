//class for the styiling purposses of a wrapper to avoid redundancy and unnecessary div

//hoc higher order component

import React from "react";
const aux = (props) => props.children;
export default aux;
